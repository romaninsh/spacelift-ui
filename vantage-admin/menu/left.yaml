# yaml-language-server: $schema=./menu-schema-1.json
items:
  - { page: apps, label: Apps }
  - { page: deployments, label: Deployments }
  - { page: environments, label: Environments }
  - { page: pull-requests, label: Pull requests }
  # Runs and Resources stay unlisted: they exist so a stack's relation tabs
  # have a page to drill into, not as destinations of their own.
  - section: SpaceLift
    children:
      - { page: spacelift-stacks, label: Stacks }
